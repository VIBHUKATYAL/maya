name: Independent Cron Trigger
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping-cron:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Master Discovery Engine (Scaled Vercel Safety Loop)
        run: |
          curl -X GET "https://${{ secrets.VERCEL_DOMAIN }}/api/agent/cron"
